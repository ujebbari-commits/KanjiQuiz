# KanjiQuiz Web

Firefox上でYomitanを使いながら遊べる、KanjiQuizのWeb版です。

## 主な機能

- JSON / CSV / TSV / TXTのデッキ読み込み
- 通常、サバイバル、タイムアタック、3回正解、苦手語、デイリー
- 間違いを全問正解するまで繰り返す定着復習
- 逆4択と類似したダミー選択肢
- 1問ごとの最大挑戦回数
- 履歴、正答率グラフ、苦手度、連続日数
- めくりモード
- オフラインPWA
- 問題文と正解文を通常のHTMLテキストで表示し、Firefox/Yomitanで選択可能

## 対応データ

### KanjiQuiz JSON

```json
{
  "format": "KanjiQuizWebDeck",
  "version": 1,
  "deck": {
    "name": "難読漢字",
    "fields": ["漢字", "読み", "メモ"],
    "notes": [
      {"id": "1", "fields": ["薔薇", "ばら", "植物"]}
    ]
  }
}
```

### CSV / TSV / TXT

先頭行をフィールド名として読み込めます。Anki Desktopのテキスト書き出しはTSVとして読み込めます。AnkiDroidから移す場合は、Android版v1.12のデッキ設定画面で「Web版用JSONを保存」を押してください。

## GitHub Pages

`.github/workflows/pages.yml`が`web/`をGitHub Pagesへ公開します。
リポジトリの Settings → Pages → Source を `GitHub Actions` に設定してください。

公開URLは通常、次の形式です。

`https://<GitHubユーザー名>.github.io/<リポジトリ名>/`


## v0.1.1

Android版Firefoxのファイル選択で `File.text()` が空文字を返す場合に備え、ArrayBufferとFileReaderによる読み込みへ自動的にフォールバックします。選択ファイルが0バイトとして渡された場合は、ファイル管理アプリでDownloadへコピーまたは別名保存する案内を表示します。


## Web v0.1.2

「3回正解」のカード別進捗をlocalStorageへ保存します。正解で+1、不正解で-1となり、3に到達したカードは同じ設定では出題されません。


## Web v0.1.3

ゲーム画面の文字サイズを16〜96pxで設定できます。クイズ中とめくり中も A− / A＋ で変更でき、値はFirefox内に保存されます。


## Web v0.1.5

- 入力欄のフォーカスだけでキーボード表示中と誤判定し、タイマーやパス等が消える問題を修正。
- 実際にViewportが縮んだ場合だけコンパクト表示へ切り替える。
- タイマー、進捗、パス、スコア、文字サイズ操作は常時表示し、問題文領域のみ必要に応じてスクロール可能にした。


## Web v0.1.6

- 再挑戦しても同じ問題の残り時間を引き継ぎます。
- Firefox Androidでキーボード表示中に回答・パスをタップした際、最初のタップで確実に処理されるよう修正しました。

## Web v0.1.7

- 全クイズモードで共有する成功回数設定を追加。
- 成功3回のカードを全クイズモードから除外。
- カード別成功回数画面で検索・確認・カード単位リセットが可能。

## Web v0.1.8

- 全クイズ形式で「新規のみ」を利用可能。カードを問題として表示した時点で新規状態を解除し、Firefox内へ保存します。
- 複数デッキを混ぜる仮想「デイリーデッキ」を追加しました。
- デイリー目標はカード枚数またはプレイ時間から選択できます。
- デイリー対象デッキを複数選択でき、未完了日はホームのデイリーカードが光ります。
- 枚数目標に必要なカードが不足している場合は、目標を自動縮小せずエラーを表示します。


## Web v0.1.9

- 「今日のデイリー」の下部ボタンを「設定して始める」に変更しました。
- 枚数制デイリーは正解カードだけを数え、不正解・パスしたカードを後で再出題します。
- パス後は答えを表示し、自動送りを行わず「次へ」を待ちます。


## Web v0.1.10

- Firefox Androidでパス・回答を短くタップした際、答え表示直後の互換clickが「次へ」に当たり、即座に次のカードへ進む問題を修正。
- touch/pen操作はpointerupで1回だけ確定し、描画後のボタンへのゴーストクリックを抑止する。

## Web v0.1.13

- 既存デッキの設定画面に「更新したデッキを再取り込み」を追加しました。
- Android版から再出力したJSONのカードIDと既存カードIDを照合し、成功回数、苦手度、出題済み状態などを維持したまま内容を更新します。
- 保存前に、進捗を維持する既存カード、新規追加、内容変更、削除されるカードの枚数を表示します。
- 新規カードは進捗0から開始します。新データから消えたカードはデッキ一覧から外れますが、進捗データは内部に残り、同じカードIDで再追加すると復元されます。
- CSV・TSVなど安定したカードIDがない形式は、安全な更新取り込みの対象外です。

## Web v0.1.15
- 時間制デイリーの全体残り時間は設定で表示切替でき、既定では非表示。
- 各問の制限時間が有効な場合、「現在の問題」の残り時間バーは独立して表示する。


## Web v0.1.16

- 完了済みのデイリーを、ホームの「もう一度デイリー」から同じ日に再プレイ可能。
- 完了状態は維持し、追加練習として扱う。


## Web v0.1.17

- デイリー中の左上表示を「デイリー・回答 N問」に変更し、進捗バーとの情報重複を解消。バーの表示・計算ロジックは変更なし。
